---
title: "How to Install Hermes Agent: Complete Guide for All Platforms"
titleZh: "Hermes Agent 完整安装指南：支持所有平台"
slug: "installation"
description: "Step-by-step installation guide for Hermes Agent on Linux, macOS, WSL2, and Android Termux."
descriptionZh: "Hermes Agent 在 Linux、macOS、WSL2 和 Android Termux 上的安装教程。"
author: "HermesAgent Community"
publishedAt: "2026-04-15"
updatedAt: "2026-04-15"
category: "getting-started"
tags: ["installation", "beginner", "setup", "linux", "macos"]
difficulty: "beginner"
readingTime: 8
series: "getting-started"
seriesOrder: 1
featured: true
---

## Prerequisites

Before installing Hermes Agent, make sure your system meets these requirements:

- **Operating System**: Linux (Ubuntu 20.04+, Debian 11+), macOS 12+, WSL2, or Android Termux
- **Node.js**: v18 or higher
- **Git**: latest version
- **An AI API key**: OpenRouter, OpenAI, Anthropic, or any OpenAI-compatible provider

> **Tip**: Don't have an API key yet? We recommend [OpenRouter](https://openrouter.ai) — it gives you access to 200+ models with a single key.

## Quick Install (Recommended)

The fastest way to get Hermes Agent running:

```bash
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/install.sh | bash
```

This script will:
1. Check your system requirements
2. Install Hermes Agent globally
3. Create a default configuration
4. Run the initial setup wizard

After installation, verify it's working:

```bash
hermes --version
# Expected output: hermes-agent v0.8.x
```

## Manual Installation

If you prefer manual installation or the quick script didn't work:

### Step 1: Clone the Repository

```bash
git clone https://github.com/NousResearch/hermes-agent.git
cd hermes-agent
```

### Step 2: Install Dependencies

```bash
npm install
```

### Step 3: Configure Your API Key

Create a `.env` file in the project root:

```bash
cp .env.example .env
```

Edit `.env` and add your API key:

```env
OPENROUTER_API_KEY=sk-your-api-key-here
DEFAULT_MODEL=anthropic/claude-3.5-sonnet
```

### Step 4: Start Hermes

```bash
npm start
```

You should see:

```
🚀 Hermes Agent is active and ready.
Type your message or use /help for commands.
```

## Platform-Specific Notes

### macOS

On macOS, you may need to install Xcode Command Line Tools first:

```bash
xcode-select --install
```

### WSL2 (Windows)

Make sure you're using WSL2, not WSL1:

```bash
wsl --set-default-version 2
```

Then follow the Linux installation steps inside your WSL2 terminal.

### Android Termux

```bash
pkg install nodejs git
```

Then follow the manual installation steps.

## Troubleshooting

### Error: "Permission denied"

```bash
chmod +x install.sh
sudo ./install.sh
```

### Error: "Node.js version too old"

Update Node.js using nvm:

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
nvm use 18
```

### Error: "API key invalid"

Double-check your API key in `.env`. Make sure there are no extra spaces or quotes.

## Next Steps

Now that Hermes is installed, you're ready to:

- **[Have your first conversation](/en/tutorials/getting-started/first-conversation)** — Learn the basics of chatting with Hermes
- **[Choose the right AI model](/en/tutorials/getting-started/choosing-a-model)** — Compare models and optimize for your use case
- **[Master CLI commands](/en/tutorials/getting-started/cli-essentials)** — Essential commands every user should know

---

*Last updated: April 15, 2026 · Hermes Agent v0.8*
